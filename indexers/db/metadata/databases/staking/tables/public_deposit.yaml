table:
  name: deposit
  schema: public
select_permissions:
  - role: user
    permission:
      columns:
        - id
        - status
        - created_at
        - domain_block_number_deposited_at
        - epoch_deposited_at
        - staked_at
        - updated_at
        - amount
        - storage_fee_deposit
        - total_amount
        - total_withdrawn
        - account_id
        - domain_id
        - extrinsic_hash
        - nominator_id
        - operator_id
        - timestamp
      filter: {}
      allow_aggregations: true
    comment: ""
