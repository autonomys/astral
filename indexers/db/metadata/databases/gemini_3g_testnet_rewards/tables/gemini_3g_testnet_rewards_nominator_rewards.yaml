table:
  name: nominator_rewards
  schema: gemini_3g_testnet_rewards
select_permissions:
  - role: user
    permission:
      columns:
        - _block_range
        - created_at
        - updated_at
        - amount
        - nominator_current_shares
        - operator_current_total_shares
        - operator_reward
        - id
        - nominator_id
        - operator_id
        - _id
      filter: {}
      allow_aggregations: true
    comment: ""
