table:
  name: operator_states
  schema: gemini_3g_testnet_rewards
select_permissions:
  - role: user
    permission:
      columns:
        - _block_range
        - created_at
        - updated_at
        - current_total_shares
        - current_total_stake
        - id
        - operator_id
        - _id
      filter: {}
      allow_aggregations: true
    comment: ""
