table:
  name: account_histories
  schema: consensus
object_relationships:
  - name: account
    using:
      manual_configuration:
        column_mapping:
          id: id
        insertion_order: null
        remote_table:
          name: accounts
          schema: consensus
select_permissions:
  - role: user
    permission:
      columns:
        - _block_range
        - created_at
        - free
        - nonce
        - reserved
        - total
        - updated_at
        - id
        - _id
      filter: {}
    comment: ""
