table:
  name: transfers
  schema: consensus
configuration:
  column_config:
    _id:
      custom_name: uuid
  custom_column_names:
    _id: uuid
  custom_root_fields: {}
select_permissions:
  - role: user
    permission:
      columns:
        - _block_range
        - _id
        - created_at
        - date
        - event_id
        - extrinsic_id
        - fee
        - from
        - id
        - success
        - timestamp
        - to
        - value
      filter: {}
      allow_aggregations: true
    comment: ""
