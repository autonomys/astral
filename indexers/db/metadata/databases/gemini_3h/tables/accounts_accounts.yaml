table:
  name: accounts
  schema: accounts
select_permissions:
  - role: user
    permission:
      columns:
        - _block_range
        - created_at
        - free
        - nonce
        - reserved
        - total
        - updated_at
        - account_id
        - id
        - _id
      filter: {}
      allow_aggregations: true
    comment: ""
