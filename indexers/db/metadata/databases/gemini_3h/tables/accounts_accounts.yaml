table:
  name: accounts
  schema: accounts
select_permissions:
  - role: user
    permission:
      columns:
        - created_at
        - free
        - nonce
        - reserved
        - total
        - updated_at
        - account_id
        - id
      filter: {}
      allow_aggregations: true
    comment: ""
