table:
  name: runtimes
  schema: staking
select_permissions:
  - role: user
    permission:
      columns:
        - _block_range
        - block_height
        - created_by
        - extrinsic_id
        - id
        - name
        - sort_id
        - type
        - _id
      filter: {}
      allow_aggregations: true
    comment: ""
