table:
  name: domain_instantiations
  schema: staking
select_permissions:
  - role: user
    permission:
      columns:
        - runtime_id
        - block_height
        - created_by
        - event_id
        - extrinsic_id
        - id
        - name
        - runtime
        - runtime_info
        - sort_id
      filter: {}
      limit: 50
    comment: ""
