table:
  name: folders
  schema: files
object_relationships:
  - name: chunk
    using:
      manual_configuration:
        column_mapping:
          id: id
        insertion_order: null
        remote_table:
          name: chunks
          schema: files
  - name: cid
    using:
      manual_configuration:
        column_mapping:
          id: id
        insertion_order: null
        remote_table:
          name: cids
          schema: files
array_relationships:
  - name: folder_cids
    using:
      manual_configuration:
        column_mapping:
          id: parent_cid
        insertion_order: null
        remote_table:
          name: folder_cids
          schema: files
