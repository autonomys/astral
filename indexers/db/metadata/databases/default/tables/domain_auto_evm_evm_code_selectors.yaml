table:
  name: evm_code_selectors
  schema: domain_auto_evm
select_permissions:
  - role: user
    permission:
      columns:
        - _block_range
        - address
        - id
        - name
        - selector
        - signature
        - _id
      filter: {}
      limit: 50
      allow_aggregations: true
    comment: ""
