table:
  name: staked_unlocked_events
  schema: staking
select_permissions:
  - role: user
    permission:
      columns:
        - _block_range
        - amount
        - block_height
        - storage_fee
        - account_id
        - domain_id
        - event_id
        - extrinsic_id
        - id
        - operator_id
        - _id
      filter: {}
      limit: 50
    comment: ""
