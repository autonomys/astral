table:
  name: operator_rewards
  schema: staking
select_permissions:
  - role: user
    permission:
      columns:
        - _block_range
        - amount
        - at_block_number
        - created_at
        - extrinsic_id
        - id
        - operator_id
        - _id
      filter: {}
      allow_aggregations: true
    comment: ""
