table:
  name: operator_rewards
  schema: staking
select_permissions:
  - role: user
    permission:
      columns:
        - amount
        - at_block_number
        - block_height
        - domain_id
        - event_id
        - extrinsic_id
        - id
        - operator_id
      filter: {}
      limit: 50
    comment: ""
