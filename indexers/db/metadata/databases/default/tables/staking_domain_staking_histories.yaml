table:
  name: domain_staking_histories
  schema: staking
select_permissions:
  - role: user
    permission:
      columns:
        - current_epoch_index
        - block_height
        - current_total_shares
        - current_total_stake
        - share_price
        - domain_id
        - id
        - timestamp
      filter: {}
      limit: 50
    comment: ""
