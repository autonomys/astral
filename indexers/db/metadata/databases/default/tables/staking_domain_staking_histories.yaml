table:
  name: domain_staking_histories
  schema: staking
configuration:
  column_config:
    _id:
      custom_name: uuid
  custom_column_names:
    _id: uuid
  custom_root_fields: {}
select_permissions:
  - role: user
    permission:
      columns:
        - _block_range
        - current_epoch_index
        - block_height
        - current_total_stake
        - domain_id
        - id
        - _id
      filter: {}
    comment: ""
