table:
  name: withdraw_events
  schema: staking
select_permissions:
  - role: user
    permission:
      columns:
        - block_height
        - estimated_amount
        - shares
        - storage_fee_refund
        - account_id
        - domain_id
        - event_id
        - extrinsic_id
        - id
        - nominator_id
        - operator_id
        - sort_id
        - to_withdraw
        - timestamp
      filter: {}
      limit: 50
    comment: ""
