table:
  name: runtime_creations
  schema: staking
configuration:
  column_config:
    _id:
      custom_name: uuid
  custom_column_names:
    _id: uuid
  custom_root_fields: {}
select_permissions:
  - role: user
    permission:
      columns:
        - _block_range
        - block_height
        - created_by
        - extrinsic_id
        - id
        - name
        - sort_id
        - type
        - _id
      filter: {}
      allow_aggregations: true
    comment: ""
