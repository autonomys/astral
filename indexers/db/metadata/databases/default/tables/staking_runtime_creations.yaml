table:
  name: runtime_creations
  schema: staking
select_permissions:
  - role: user
    permission:
      columns:
        - block_height
        - created_by
        - event_id
        - extrinsic_id
        - id
        - name
        - sort_id
        - type
      filter: {}
      limit: 50
    comment: ""
