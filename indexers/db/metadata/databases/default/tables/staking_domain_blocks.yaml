table:
  name: domain_blocks
  schema: staking
select_permissions:
  - role: user
    permission:
      columns:
        - block_hash
        - block_number
        - consensus_block_hash
        - consensus_block_number
        - created_at
        - domain_epoch_id
        - domain_id
        - epoch
        - extrinsic_root
        - id
        - timestamp
        - updated_at
      filter: {}
      limit: 50
    comment: ""
