table:
  name: nominators_unlocked_events
  schema: staking
select_permissions:
  - role: user
    permission:
      columns:
        - block_height
        - domain_id
        - event_id
        - extrinsic_id
        - id
        - operator_id
      filter: {}
      limit: 50
      allow_aggregations: true
    comment: ""
