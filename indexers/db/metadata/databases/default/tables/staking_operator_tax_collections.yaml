table:
  name: operator_tax_collections
  schema: staking
select_permissions:
  - role: user
    permission:
      columns:
        - amount
        - block_height
        - domain_id
        - event_id
        - extrinsic_id
        - id
        - operator_id
      filter: {}
    comment: ""
