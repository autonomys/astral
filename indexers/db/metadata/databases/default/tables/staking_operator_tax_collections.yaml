table:
  name: operator_tax_collections
  schema: staking
select_permissions:
  - role: user
    permission:
      columns:
        - _block_range
        - amount
        - block_height
        - domain_id
        - event_id
        - extrinsic_id
        - id
        - operator_id
        - _id
      filter: {}
    comment: ""
