table:
  name: operator_registrations
  schema: staking
configuration:
  column_config:
    _id:
      custom_name: uuid
  custom_column_names:
    _id: uuid
  custom_root_fields: {}
select_permissions:
  - role: user
    permission:
      columns:
        - _block_range
        - nomination_tax
        - block_height
        - minimum_nominator_stake
        - domain_id
        - event_id
        - extrinsic_id
        - id
        - owner
        - signing_key
        - sort_id
        - _id
      filter: {}
      limit: 50
    comment: ""
