table:
  name: operator_registrations
  schema: staking
select_permissions:
  - role: user
    permission:
      columns:
        - nomination_tax
        - block_height
        - minimum_nominator_stake
        - domain_id
        - event_id
        - extrinsic_id
        - id
        - owner
        - signing_key
        - sort_id
      filter: {}
      limit: 50
    comment: ""
