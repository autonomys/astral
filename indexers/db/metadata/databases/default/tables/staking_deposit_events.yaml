table:
  name: deposit_events
  schema: staking
select_permissions:
  - role: user
    permission:
      columns:
        - account_id
        - amount
        - block_height
        - domain_id
        - estimated_shares
        - event_id
        - extrinsic_id
        - id
        - nominator_id
        - operator_id
        - sort_id
        - storage_fee_deposit
        - timestamp
        - total_amount
      filter: {}
      limit: 50
    comment: ""
