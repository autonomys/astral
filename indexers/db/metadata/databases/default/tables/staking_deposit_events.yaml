table:
  name: deposit_events
  schema: staking
configuration:
  column_config:
    _id:
      custom_name: uuid
  custom_column_names:
    _id: uuid
  custom_root_fields: {}
select_permissions:
  - role: user
    permission:
      columns:
        - _block_range
        - amount
        - block_height
        - storage_fee_deposit
        - total_amount
        - account_id
        - domain_id
        - extrinsic_id
        - id
        - nominator_id
        - operator_id
        - sort_id
        - timestamp
        - _id
      filter: {}
      limit: 50
    comment: ""
