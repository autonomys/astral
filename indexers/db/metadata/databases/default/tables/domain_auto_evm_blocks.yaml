table:
  name: blocks
  schema: domain_auto_evm
select_permissions:
  - role: user
    permission:
      columns:
        - _block_range
        - events_count
        - extrinsics_count
        - logs_count
        - transfers_count
        - height
        - transfer_value
        - author_id
        - extrinsics_root
        - hash
        - id
        - parent_hash
        - sort_id
        - spec_id
        - state_root
        - timestamp
        - _id
      filter: {}
      limit: 50
      allow_aggregations: true
    comment: ""
