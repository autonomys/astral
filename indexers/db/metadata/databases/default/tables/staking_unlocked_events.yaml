table:
  name: unlocked_events
  schema: staking
select_permissions:
  - role: user
    permission:
      columns:
        - amount
        - block_height
        - storage_fee
        - account_id
        - domain_id
        - event_id
        - extrinsic_id
        - id
        - nominator_id
        - operator_id
      filter: {}
      limit: 50
    comment: ""
