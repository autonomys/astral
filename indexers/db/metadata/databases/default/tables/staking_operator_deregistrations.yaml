table:
  name: operator_deregistrations
  schema: staking
select_permissions:
  - role: user
    permission:
      columns:
        - _block_range
        - block_height
        - domain_id
        - event_id
        - extrinsic_id
        - id
        - owner
        - _id
      filter: {}
      limit: 50
    comment: ""
