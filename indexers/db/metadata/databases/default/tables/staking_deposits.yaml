table:
  name: deposits
  schema: staking
select_permissions:
  - role: user
    permission:
      columns:
        - _block_range
        - amount
        - created_at
        - storage_fee_deposit
        - total_amount
        - account_id
        - domain_id
        - extrinsic_id
        - id
        - nominator_id
        - operator_id
        - sort_id
        - _id
      filter: {}
      allow_aggregations: true
    comment: ""
