{
  "name": "indexers",
  "version": "1.0.0",
  "private": true,
  "main": "index.js",
  "license": "MIT",
  "workspaces": [
    "gemini-3h/*",
    "gemini-3g/*",
    "db"
  ],
  "scripts": {
    "dev": "lerna run codegen && lerna run build && docker compose -f ../docker-compose.yml up --remove-orphans",
    "prod": "lerna run codegen && lerna run build && docker compose -f ../docker-compose.yml  -f ../docker-compose.prod.yml up -d up --remove-orphans",
    "build": "lerna run build",
    "codegen": "lerna run codegen",
    "prepack": "lerna run prepack",
    "bootstrap": "lerna run bootstrap"
  },
  "dependencies": {
    "lerna": "^8.1.8"
  }
}
